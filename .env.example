# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration (REQUIRED - Generate a strong secret!)
JWT_SECRET=supersecretkeychangemeinproduction
JWT_EXPIRES_IN=1h

# CORS Configuration
CORS_ORIGIN=*

# Mock User Credentials (In production, use a database)
# Format: username:password:userId (comma separated for multiple users)
MOCK_USERS=admin:password123:1,user:password:2